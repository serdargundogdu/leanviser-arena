"""Shared kernel — cross-cutting primitives with no layer dependencies.

Reserved for small, framework-free helpers/types used across layers. Empty in
v0.1; kept as an explicit seam so shared concepts have an obvious home.
"""
