"""Scoring domain — the composite Arena score.

Pure and deterministic. Rewards FLOW (short lead time, high flow efficiency,
reliable delivery); throughput is deliberately excluded. Server-authoritative:
the client never computes the score.
"""
