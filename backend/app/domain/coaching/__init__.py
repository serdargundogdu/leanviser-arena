"""Coaching domain — turns flow metrics into actionable, lever-tied guidance.

Pure and rule-based (no ML). Produces language-neutral ``Insight`` codes; the UI
localizes them to Turkish. Coaching only INTERPRETS a run — it never changes the
simulation or the score.
"""
