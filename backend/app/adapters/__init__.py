"""Adapters layer — driving/driven adapters (ports & adapters).

FastAPI HTTP, persistence, and outbound integrations belong here. v0.1 ships
no concrete adapters beyond the FastAPI app in ``app.main``; DB/auth and the
LeanViser Core port are deferred to later slices.
"""
